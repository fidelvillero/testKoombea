<%= form_for(@links) do |f| %>
  <% if @links.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@links.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @links.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
  
  
<% end %>
<%= javascript_include_tag :defaults %>
<%= javascript_include_tiny_mce_if_used %>
<%= tiny_mce if using_tiny_mce? %>

<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
  <script type = "javascripts">
  var valText = "^(ht|f)tp(s?)\:\/\/[0-9a-zA-Z]([-.\w]*[0-9a-zA-Z])*(:(0-9)*)*(\/?)( [a-zA-Z0-9\-\.\?\,\'\/\\\+&%\$#_]*)?$"
    
    Function testUrl(textArea){
      if ((textArea.value.match(valText)) && (textArea.value!='')) {  
        alert('Password Correcta');   
      }
      
    } 
  </script>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :class => "mceEditor" %>
  </div>
  <div class="field">
    <%= f.label :url_related %><br />
    <%= f.text_area :url, :class => "mceEditor" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
